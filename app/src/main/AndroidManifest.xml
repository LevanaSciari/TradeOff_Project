<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tradeoff">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyNotificationActivity"></activity>
        <activity android:name=".DiagramGive" />
        <activity android:name=".DiagramRegion" />
        <activity android:name=".DiagramTake"></activity>
        <activity android:name=".Delete_Post_Admin" />
        <activity
            android:name=".changeCategory"
            android:label="@string/title_activity_category"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MyPost" />
        <activity android:name=".Search" />
        <activity android:name=".EditProfile" />
        <activity android:name=".Profile" />
        <activity android:name=".CreatePost" />
        <activity android:name=".Home" />
        <activity android:name=".Forgot_Password" />
        <activity android:name=".RegisterUser" />
        <activity android:name=".Administrator" /> <!-- notificatin service -->
        <service
            android:name=".notification"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="your.app.domain.NotificationService" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>